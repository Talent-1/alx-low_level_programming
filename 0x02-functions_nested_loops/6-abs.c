#include "main.h"

/**
 * _abs - computes the value of an interger
 *@t: the integer to be computed
 *
 * Return: absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
